[common info]
baseURL = https://www.unipro.io/
; browser: single value (e.g. chrome) or comma-separated list for cross-browser runs (e.g. chrome, firefox)
browser = chrome
headless = true
page_load_timeout = 25